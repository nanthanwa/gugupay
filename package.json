{
  "name": "gugupay-monorepo",
  "version": "0.0.1",
  "private": true,
  "license": "Apache-2.0",
  "workspaces": [
    "./sdk",
    "./web"
  ],
  "scripts": {
    "sdk:build": "npm --workspace=sdk run compile",
    "web": "npm --workspace=web run dev",
    "web:build": "npm --workspace=web run build"
  }
}
